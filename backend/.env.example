# Solar Installer AI - Backend Environment Configuration
# Copy this file to .env and update with your actual values

# ── Email Configuration ────────────────────────────────────────────
# Gmail SMTP configuration for sending procurement emails
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-gmail-app-password

# ── Environment Configuration ──────────────────────────────────────
# Application environment (development, staging, production)
ENVIRONMENT=development

# API server configuration
API_HOST=127.0.0.1
API_PORT=8003

# CORS configuration
ALLOW_ALL_ORIGINS=false
PUBLIC_DEPLOYMENT=false
NETWORK_DEPLOYMENT=false

# ── AI/ML Configuration ─────────────────────────────────────────────
# OpenAI API key for document processing and AI features
OPENAI_API_KEY=your-openai-api-key-here

# ── Google Sheets Configuration ────────────────────────────────────
# Google Sheets integration (credentials file path is hardcoded)
# Place your service account JSON file at: backend/sheets_credentials.json
GOOGLE_SHEETS_ENABLED=true

# ── Authentication Configuration ───────────────────────────────────
# Simple username/password authentication
AUTH_USERNAME=admin
AUTH_PASSWORD=your-secure-password

# ── Database Configuration (Optional) ──────────────────────────────
# If using a database instead of Excel/Google Sheets
# DATABASE_URL=postgresql://user:password@localhost:5432/solar_installer
# REDIS_URL=redis://localhost:6379

# ── Logging Configuration ──────────────────────────────────────────
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# ── Security Configuration ─────────────────────────────────────────
# Secret key for session management
SECRET_KEY=your-secret-key-here

# ── WhatsApp Integration (Optional) ────────────────────────────────
# WHATSAPP_API_KEY=your-whatsapp-api-key
# WHATSAPP_PHONE_NUMBER=your-phone-number
